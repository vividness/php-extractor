language: c
compiler:
  - gcc
  - clang

before_install:
  - "sudo apt-get install php-dev"
  - "sudo apt-get install libextractor-dev"

script:
  - "phpize"
  - "./configure"
  - "make"
  - "make test" 
